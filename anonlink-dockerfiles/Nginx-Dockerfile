FROM kierk/anonlink-nginx:latest

LABEL maintainer="Drew Pellitieri <apellitieri@mitre.org>"

USER root

RUN chmod -R ugo+rwx /etc/nginx
RUN chmod -R ugo+rwx /var/cache/nginx
RUN chmod -R ugo+rwx /var/run

# USER
