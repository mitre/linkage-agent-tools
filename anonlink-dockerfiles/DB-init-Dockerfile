FROM data61/anonlink-app:latest

LABEL maintainer="Drew Pellitieri <apellitieri@mitre.org>"

USER root

ENTRYPOINT /bin/sh -c "dockerize -wait tcp://db:5432 alembic upgrade head"

RUN chmod -R ugo+rwx /var/www
RUN chmod -R ugo+rwx /var/log
